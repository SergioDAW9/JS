/*
* Métodos necesarios
*/

let=resultado
resultado=Math.PI;

// Redondear resultados

resultado=Math.round(2.8); // 3
resultado=Math.round(2.5); // 3

// Funcion suelo y funcion techo

resultado=Math.ceil(2.1); // 2

resultado=Math.floor(2.3) //3

// Operaciones avanzadas

resultado=Math.sqrt(34); // Raiz cuadrada

resultado=Math.abs(-35); //Valor entero

resultado=Math.pow(2,4); // Potencia

resultado=Math.min(2,5,-1,8,-54); // Minimo de valores

resultado=Math.max(2,5,-1,8,-54); // Maximo de valores

resultado=Math.random(); // Numero aleatorio

resultado=Math.floor(Math.random()*30); // Valor entero comprendido entre 0 - 30
