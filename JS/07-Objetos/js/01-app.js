//Object literal, esquema de construccion de un objeto

const producto={
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible:true
};

// Muestra el objeto completo
console.log(producto);

// Muestra especificamente un atributo del objeto
console.log(producto.precio);
console.log(producto['nombre']); 